public class SerializObject {

	public void bind() throws Exception{
	//create objectr to be bound
	Task cleanHouse= new Task("Clean House");	
	//ObjectOutputStream oos = new ObjectOutputStream(cleanHouse);

	//perform the bound
	//ctx.bind("cn=Button", b);
	//Context ctx = new InitialContext();
	//ctx.bind("name", cleanHouse);

	}
}